***Simple CRUD operation using Node and Express***

2. **Install dependencies**  
   Run the following command:

   ```bash
   npm install
   
3. **Start Server**
    To start the backend, run:

      ```bash
   npm run start
